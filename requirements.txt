# Core Airflow (assume Airflow itself is already installed on the target
# environment / MWAA / Composer image - listed here for local dev only)
apache-airflow>=2.7.0,<2.10.0
apache-airflow-providers-amazon>=8.13.0
apache-airflow-providers-http>=4.9.0

# Cheminformatics
rdkit>=2023.9.1

# Data & ML
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Notifications
requests>=2.31.0

# Config
PyYAML>=6.0

# ---- Optional extras (Step 4 & 5) ----
# Install with: pip install -r requirements-optional.txt
# chemprop>=1.6.1
# faerun>=0.4.7
# tmap>=1.2.1
